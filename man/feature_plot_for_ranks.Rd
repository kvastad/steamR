% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature_plot_for_ranks.R
\name{feature_plot_for_ranks}
\alias{feature_plot_for_ranks}
\title{Visualize Rank-Based Enrichment Scores with Spatial Feature Plots}
\usage{
feature_plot_for_ranks(
  se,
  window_rank_list,
  ot_gene_set_label,
  disease_abbr,
  cluster_number = NULL,
  ranks_per_plot = 6,
  spot_alpha = 0.5,
  point_size = 1,
  all_clusters = FALSE,
  cluster_anno = "cluster_anno",
  seurat_type = NULL
)
}
\arguments{
\item{se}{A Seurat object with spatial transcriptomics data and precomputed enrichment scores.}

\item{window_rank_list}{A list of gene rank windows used for enrichment calculation.}

\item{ot_gene_set_label}{Character. The gene set label (e.g., "Genetic").}

\item{disease_abbr}{Character. Abbreviation for the disease/trait (e.g., "SCZ").}

\item{cluster_number}{Optional. A specific cluster to visualize (used when \code{all_clusters = FALSE}).}

\item{ranks_per_plot}{Number of rank plots to show per panel (default is \code{6}).}

\item{spot_alpha}{Alpha value for spot transparency in plots (default is \code{0.5}).}

\item{point_size}{Point size for spatial plots (default is \code{1}).}

\item{all_clusters}{Logical. Whether to visualize all clusters together (default is \code{FALSE}).}

\item{cluster_anno}{Name of the metadata column to use for coloring (default: "cluster_anno").}

\item{seurat_type}{Optional. Type of Seurat object ("seurat" or "stutility"). If NULL, will attempt to auto-detect.}
}
\value{
No return value. Generates spatial enrichment plots per cluster/rank.
}
\description{
Generates spatial visualizations for enrichment scores computed over ranked gene windows.
Works with both standard Seurat objects and STUtility-generated Seurat objects.
}
\examples{
feature_plot_for_ranks(
  se = se,
  window_rank_list = window50_rank_list_SCZ_Genetic,
  ot_gene_set_label = "Genetic",
  disease_abbr = "SCZ",
  cluster_number = 5,
  ranks_per_plot = 4,
  all_clusters = FALSE
)
}
