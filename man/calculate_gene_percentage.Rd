% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_gene_percentage.R
\name{calculate_gene_percentage}
\alias{calculate_gene_percentage}
\title{Calculate Gene Percentage}
\usage{
calculate_gene_percentage(se, pattern)
}
\arguments{
\item{se}{A `Seurat` object containing the gene expression data.}

\item{pattern}{A regular expression pattern to match gene names (e.g., "^mt-" for mitochondrial genes).}
}
\value{
A numeric vector with percentages, where each value corresponds to the percentage 
        of counts from the matched genes relative to the total counts in a given column.
}
\description{
This function calculates the percentage of a specific set of genes (defined by a pattern)
across the total counts in each column (e.g., spatial spots or cells) of a Seurat object.
}
\details{
The function identifies genes matching the provided pattern in the row names of the `Seurat` object.
For each column, it computes the total counts for these genes and divides by the total counts 
for all genes, multiplying by 100 to convert to a percentage.
}
\examples{
# Example with mock Seurat object
library(Seurat)
mock_data <- matrix(data = c(10, 0, 5, 0, 20, 0, 0, 5, 0, 0, 0, 0), nrow = 4)
rownames(mock_data) <- c("mt-ATP6", "mt-ND1", "Rpl10", "Rps5")
colnames(mock_data) <- paste0("Spot", 1:3)
se <- CreateSeuratObject(counts = mock_data)

result <- calculate_gene_percentage(se, "^mt-")
print(result)  # [1] 100   0  50

}
